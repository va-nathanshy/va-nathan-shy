/// Write a function in Dart named add that takes two numbers as arguments and returns their sum.
num add(num addend1, num addend2) => addend1 + addend2;


void main() {
  num addend1 = 10;
  num addend2 = 20;

  print(add(addend1, addend2));
}